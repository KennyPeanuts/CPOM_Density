setwd("~/current_research/CPOM_Density/lab_notebook")
survey <- read.delim("./data/CPOM_survey_2013_calc.csv", header = T, stringsAsFactors = F, sep = ",")
sd(survey$sed.propOM)
sd(survey$sed.propOM, na.rm)
sd(survey$sed.propOM, na.rm = T)
lake.LOG.loi.aov <- aov(log10(sed.propOM) ~ lake, data = survey)
summary(lake.LOG.loi.aov)
TukeyHSD(lake.LOG.loi.aov)
lake.loi.aov <- aov(sed.propOM ~ lake, data = survey)
summary(lake.loi.aov)
TukeyHSD(lake.loi.aov)
plot(lake.loi.aov)
plot(lake.LOG.loi.aov)
plot(log10(sed.propOM ~ lake, data = survey))
plot(log10(sed.propOM ~ lake, data = survey))
plot(log10(sed.propOM) ~ lake, data = survey)
plot(log10(sed.propOM))
plot(log10(survey$sed.propOM))
plot(log10(survey$sed.propOM), survey$lake)
